---
topic: PJAN-77 Project Notebook companion memory for PJangler repositories
updated: 2026-08-19T15:31
---

- (direction) Headless create mode with slug project-notebook; distill the finalized PRD and keep architecture-driving addendum content load-bearing.
- (note) Configured persistent fact project-context.md is absent at the project root; the finalized PRD and addendum already reconcile current brownfield evidence and remain the input authority.
- (decision) SPEC.md will absorb the finalized PRD into eight stable capabilities; the PRD becomes an audit source, not a downstream companion.
- (decision) The PRD addendum remains an adopted companion because its configuration examples, command grammar, service adapter rules, hook flow, and verification matrix still bend architecture decisions.
- (decision) acceptance-contract.md is a spec-authored companion that preserves detailed behavioral outcomes and capability-to-FR traceability without bloating the kernel.
- (capability) CAP-1 intent: an operator can bootstrap or link each registered repository to exactly one Companion Notebook through the normal PJangler plan, apply, and live-action lifecycle; success requires zero-write dry-run, exactly one notebook and Overview Note after authorized apply, registry persistence only after truthful remote outcome, recoverable planned state on failure, and duplicate-free retries.
- (capability) CAP-2 intent: PJangler can resolve and report an authoritative Notebook Binding plus effective policy from global and repository configuration; success requires Registry authority for binding fields, Manifest read-only binding projection and policy overrides, source-attributed effective values, Drift on mismatch, both YAML and PostgreSQL round-trip, and preservation of unknown fields.
- (capability) CAP-3 intent: users and automation can perform repository-scoped status, create, Overview Note, note CRUD, capture recovery, and search operations through pj notebook; success requires stable note IDs, deterministic pagination, confirmed deletion, Overview protection, notebook-only results, versioned clean JSON, readable human output, and categorized errors.
- (capability) CAP-4 intent: operators can audit and migrate only Project Notebook-owned state; success requires side-effect-free local or authorized remote findings, selective owned-rule planning, preservation of foreign state, verified postconditions, truthful recovery on failure, and a byte-and-remote no-op second run.
- (capability) CAP-5 intent: PJangler can communicate with an Open Notebook-compatible service through one safe reusable boundary; success requires runtime-only endpoint and authentication, finite bounds, normalized errors, safe retry rules, reconciliation after ambiguous create, stable-ID ownership checks, and zero cross-project results or mutations.
- (capability) CAP-6 intent: PJangler can install one canonical Project Notebook skill and register repository-scoped Managed Hooks through master fanout; success requires action only for enabled bindings, preservation of foreign hook data and ordering, idempotent projection, Claude Code true SessionStart and SessionEnd support, and rejection of turn-level Stop as session close.
- (capability) CAP-7 intent: a supported agent session can receive its bounded Overview Note once while recording a trustworthy baseline; success requires once-per-session emission, separate labeling from Hindsight, configured size and time bounds, baseline capture even when retrieval is disabled or fails, and fail-open behavior.
- (capability) CAP-8 intent: a true session-close boundary can preserve one factual Session Capture and current derivatives of eligible changed documents without delaying shutdown; success requires atomic deduplicated receipt enqueue, baseline-to-close Git evidence, documented exclusions, path and digest provenance, bounded low-cost summarization with deterministic fallback, finite retries, and observable receipt states.
- (constraint) Git and version-controlled repository documents remain authoritative; Companion Notebook content, synchronized derivatives, Overview Note, and summaries are derivative context only.
- (constraint) One canonical repository identity maps to one stable remote notebook identifier; repository name is only a deterministic mutable display name, and rename Drift must not create a second notebook.
- (constraint) The singleton recipe registry owns Project Notebook lifecycle checks and orchestration; CLI, future MCP surfaces, and hook entrypoints remain thin adapters to one module API.
- (constraint) The Project Registry owns global defaults and binding state, the Project Manifest owns repository policy and mirrors binding read-only, the Notebook Service owns note bodies, and Git owns source documents.
- (constraint) Dry-run performs zero local and remote writes; composite init, create, and migrate remote mutations require --live, direct note mutations are authorized by the invoked command with destructive confirmation, and hook reads or writes require durable per-repository policy.
- (constraint) MVP must round-trip Notebook Binding fields through YAML with PJ_PROJECT_REGISTRY override and PostgreSQL RegistryStore using additive migration while preserving unknown and unrelated fields.
- (constraint) Service configuration uses an explicitly configured hostname and runtime-only authentication; no LAN address, interactive-only default, or raw credential may enter tracked configuration, persistent registry data, logs, exceptions, fixtures, hook payloads, JSON, or notes.
- (constraint) Managed Hooks fail open, use true lifecycle boundaries, keep atomic permission-restricted mutable state outside the repository, and never treat a turn-level stop event as session close.
- (constraint) All context, excerpts, diff inputs, response bodies, uploads, diagnostics, network calls, and retries are bounded; documentation-only capture is default and secret-like, ignored, generated, binary, or oversized files are excluded observably.
- (constraint) Upstream search is treated as global and untrusted; every result and note operation must prove membership in the resolved Notebook Binding before disclosure or mutation.
- (constraint) Implementation remains compatible with PJangler Node.js 20+ TypeScript ESM, the existing plan/apply transaction, singleton recipe catalog, current hook fanout, and isolated fake-service testing.
- (constraint) Every logical init, create, hook delivery, document revision, Session Capture, and migration must be idempotent and preserve unrelated content.
- (assumption) A-1: Overview Note content defaults to a 4000-character limit and remains configurable only within a separate safe ceiling.
- (assumption) A-2: Capture quality is accepted against at least 20 staged sessions with complete provenance and at least 95 percent factual claim support.
- (assumption) A-3: A configured low-cost LLM is normally available, but absence or failure never prevents deterministic fallback capture.
- (assumption) A-4: Verified Open Notebook v1.14 notes CRUD, disabled password authentication, and unfiltered search remain sufficiently compatible; adapter contract tests, not the version string, are authoritative.
- (assumption) A-5: MVP serves one trusted operator through one shared Notebook Service; multi-tenant authorization is deferred.
- (assumption) A-6: Initial p95 budgets are two seconds for session-start priming and 250 milliseconds for session-close foreground enqueue.
- (decision) There are no unresolved MVP open questions; MCP parity, richer overview generation, unsupported agent clients, multi-tenant controls, and fleet-wide workflows are explicit post-MVP scope.
- (note) Acceptance preservation for CAP-1: dry-run reports binding, configuration, hooks, Overview Note, and live actions with zero writes; authorized generated-project apply creates exactly one notebook and overview, persists registry last, and repeated or ambiguous retries remain duplicate-free.
- (note) Acceptance preservation for CAP-2: status distinguishes persisted disabled, planned, and linked from computed unconfigured, healthy, drifted, unavailable, and blocked; local-only never contacts the service; configuration reports value provenance without secrets and rejects Manifest mirror drift.
- (note) Acceptance preservation for CAP-3: note create returns stable ID; get, update, and delete validate notebook ownership; list ordering and pagination are deterministic; Overview deletion is rejected; empty search succeeds; JSON schema version 1 separates stdout data from stderr diagnostics and exposes symbolic error categories.
- (note) Acceptance preservation for CAP-4: audit uses pass, fail, warn, and skip and covers config, binding, remote identity, overview, skill, hooks, and receipts; migrate selects only public Project Notebook rules, separates local from live work, blocks ambiguity, verifies postconditions, and makes the second run a no-op.
- (note) Acceptance preservation for CAP-5: service calls normalize invalid input, not configured, authentication, not found, conflict, throttling, timeout, unavailable, malformed protocol, cross-project, and Drift outcomes; only reads and proven-idempotent writes retry automatically.
- (note) Acceptance preservation for CAP-6 and CAP-7: canonical global assets project without duplicate hooks or loss of foreign records; repository enablement scopes behavior; supported start records baseline and emits one bounded labeled Overview Note, while missing config, auth, overview, timeout, or malformed response remains non-blocking.
- (note) Acceptance preservation for CAP-8: foreground close writes one receipt then returns; missing baseline blocks document upload and requires explicit Git ref; eligible documents derive from version-control evidence and record path, revision or digest, session, time, and policy; summaries separate observed changes, verification, and unresolved work without unsupported deployment claims.
- (note) Capture receipts expose queued, processing, succeeded, failed, retry-exhausted, and blocked-missing-baseline; retry and retention are finite, successful receipts expire by policy, and unresolved receipts remain visible until recovery or explicit dismissal.
- (note) Validation evidence must cover zero-write plan, fresh live apply, ambiguous create retry, both registry backends, CLI success and all error categories, search contamination, supported and unsupported hooks, worker restart, deterministic fallback, security adversaries, path and symlink escape, packaging, Node 20+, and no live production dependency.
- (note) Success is demonstrated by one healthy init plan and live apply under two minutes, exact one-to-one notebook and Overview Note, passing module audit, 100 percent CRUD and search contract cases, duplicate-free retries and migration, fail-open injected hook failures, zero secret disclosure, zero cross-project results, and evidence-grounded staged capture quality.
- (note) Counter-signals: note volume, prompt volume, green local state without remote proof, and cosmetically fresh but unsupported summaries are not success.
- (decision) Explicit non-goals: no authority replacement, cross-project or fleet search and migration, analytics, rollups, knowledge graph, Notebook Service administration, GUI, full-source or unbounded transcript export, rich-media ingestion, new MCP surface, unsupported-client equivalence, silent mass migration, advanced outbox platform, or multi-tenant controls in MVP.
- (event) Coherence validation PASS: eight unique capabilities each have WHAT-shaped intent and testable success; constraints bend design, non-goals are explicit, success signal is demonstrable, assumptions are labeled, and kernel plus companion prose remains lean.
- (note) Preservation sweep found three source consequences that needed explicit rendering: out-of-registry status recovery guidance, update revision metadata, and surfacing Overview Note Drift after referenced authoritative documents change.
- (note) Wrapper-only content intentionally dropped: PRD document-purpose prose, persona color, narrative journey framing already represented by capabilities, workflow frontmatter, reconciliation production notes, and review ceremony metadata do not change a downstream decision.
- (event) Preservation validation PASS after rendering the three explicit additions: all four journeys, FR-1 through FR-21, ten cross-cutting NFRs, six assumptions, risks, authorization rules, metrics, counter-signals, and technical addendum mechanisms land in SPEC.md or a declared companion; only logged wrapper-only content was dropped.
- (event) Spec finalized in headless create mode with eight stable capabilities, one spec-authored acceptance companion, one adopted PRD addendum companion, six assumptions, and no unresolved phase blocker or open question.
- (direction) Headless update refresh after final architecture: preserve CAP-1 through CAP-8 and adopt the final Architecture Spine as a required downstream companion.
- (decision) The final Architecture Spine binds all eight existing capabilities without adding, retiring, or renumbering a capability; it is a companion because downstream must consume its implementation-binding rules and diagrams.
- (constraint) CAP-1 and CAP-4 architecture binding: one ProjectExternalEffectPlan orders ticket-provider then Notebook then Hermes, stops after the first failed or blocked effect, and one Registry-only finalizer runs exactly once as the final mutation while externalDispatchStarted permanently forbids fresh-target deletion after dispatch begins.
- (constraint) CAP-1, CAP-3, and CAP-5 architecture binding: every notebook or note create uses fsynced RemoteMutationJournalV1 states prepared, possibly-dispatched, reconciled, and committed; only transport proof of zero dispatched bytes permits reset, unresolved dispatch reconciles only, zero may create only when safe, one adopts, and many conflicts.
- (constraint) CAP-6 architecture binding AD-23: Skillex all-skills/project-notebook remains the sole hand-edited source; build and prepack emit a symlink-free digest-manifested package export, runtime verifies source precedence, installation copies an immutable version-digest payload under XDG data, and only an owned matching global skill link may be replaced.
- (decision) CAP-7 OverviewDescriptorV1 records project identity, visible purpose or exact placeholder, ordered contained authoritative references with Git revision and content digest, and compiler policy; SessionStart compares it after baseline, labels stale content and paths as PROJECT NOTEBOOK OVERVIEW DRIFT, and owned live migration updates the same Overview note ID.
- (decision) CAP-3 user-note identity is user-note:v1 plus a UUID operation ID created in the prepared journal; a still-unresolved retry with the same binding, title, and content digest reuses that journal, while a later identical add after commit is intentionally a distinct note.
- (constraint) CAP-7 and CAP-8 share the exact lowercase-hex session key SHA-256 over the UTF-8 string pjangler-session-v1, NUL, project slug, NUL, client, NUL, nonempty client session ID; the raw client ID is never persisted, baseline is exclusive-created before Overview, and resume never overwrites it.
- (constraint) CAP-8 receipt ID and Session Capture logical ID are lowercase-hex SHA-256 over the exact UTF-8 prefixed strings pjangler-receipt-v1 plus NUL plus session key and pjangler-capture-v1 plus NUL plus session key; SessionEnd only durably enqueues, while a detached shell-free leased worker owns bounded processing and restart recovery.
- (constraint) CAP-3 and CAP-5 search uses only a complete notebook-scoped note list, NFKC plus Unicode lowercase tokenization, all distinct query tokens, deterministic title-weighted scoring and tie order; incomplete list outcomes propagate, and upstream global or semantic search is never used in v1.
- (constraint) CAP-3 JSON v1 validates one exact command-specific data schema before serialization, separates persisted binding_state from nullable observed health, emits one newline-terminated object without ANSI or progress, applies the architecture error-precedence rule, and maps stable symbolic outcomes to exits 0 through 6.
- (note) Architecture exposes the upstream exactly-once limit explicitly: a possibly-dispatched create that cannot be reconciled remains planned or blocked and never risks a blind duplicate.
- (decision) No assumptions or open questions are added by the final architecture; noncontractual ceilings, retry counts, and retention values remain centralized implementation calibration under NotebookLimitsV1, and no known architecture blocker remains.
- (event) Architecture refresh coherence validation PASS: CAP-1 through CAP-8 remain unique and unrenumbered with WHAT-shaped intent and testable success; constraints, non-goals, assumptions, and demonstrable success remain coherent and lean, and all declared paths resolve.
- (note) Architecture refresh Wrapper-only content: none dropped; the full final Architecture Spine is an adopted companion, while acceptance-contract.md restates only the seven newly exact decision edges required for story and test consumption.
- (event) Architecture refresh preservation validation PASS: the final spine is declared as a required companion and all requested bindings are explicitly preserved—AD-23 packed export, unified external tail, RemoteMutationJournalV1, OverviewDescriptorV1 Drift proof, user-note operation identity, exact session-derived hashes, and scoped local search plus JSON-v1 contract.
- (event) Spec refresh finalized in headless update mode with CAP-1 through CAP-8 unchanged, three declared companions, six existing assumptions, no new open question, and no architecture blocker.
- (direction) Headless spec refresh from the corrected final PRD and addendum; preserve CAP-1 through CAP-8 and the final Architecture Spine companion.
- (decision) Receipt-policy correction supersedes the earlier memlog phrase and any adopted Architecture Spine wording that allowed explicit dismissal: receipt dismissal is deferred and absent from v1, while every other final architecture binding remains required.
- (constraint) Succeeded Capture Receipts expire under finite configured retention; unresolved receipts remain visible until recovered and are never automatically deleted or silently compacted.
- (constraint) Configured finite positive unresolved_receipt_max_count and unresolved_receipt_max_bytes are prospective SessionEnd admission gates: a new automatic receipt is admitted only when resulting unresolved count and byte usage remain within both limits, and admission resumes only when existing recovery brings both measures below their caps.
- (constraint) When either unresolved-receipt cap prevents admission, SessionEnd exits successfully before receipt creation, states that the current session was not captured, and emits a bounded retention-pressure diagnostic and status/audit finding with current usage, both caps, and exact pj notebook capture list plus pj notebook capture retry actions.
- (decision) Retention pressure is an admission and observability condition, never a Capture Receipt state; receipt states remain queued, processing, succeeded, failed, retry-exhausted, and blocked-missing-baseline.
- (decision) One direct capture retry on an existing failed or retry-exhausted receipt authorizes exactly one further attempt using the same receipt; a failed direct attempt returns it to retry-exhausted without creating another receipt or restarting automatic retry, while blocked-missing-baseline also requires an explicit Git reference.
- (constraint) SessionStart records HEAD plus bounded tracked-document working-tree status and per-file content digests before any Overview decision; a document already dirty at start is baseline state and is captured only when end identity proves an additional in-session change.
- (constraint) Manual --baseline GIT_REF recovery proves only committed-reference comparison, never the unknown start identity of pre-existing uncommitted content; affected paths are excluded observably, and a receipt remains blocked-missing-baseline when no trustworthy evidence remains.
- (decision) Overview freshness keeps one exact v1 invariant: every Overview write records its ordered contained authoritative references and each revision or content identity; after baseline, SessionStart recomputes them and labels mismatch PROJECT NOTEBOOK OVERVIEW DRIFT with exact audit/migrate action, while audit detects and live migrate repairs the same note ID.
- (decision) The correction adds no capability, assumption, or open question; CAP-8 absorbs bounded admission and recovery behavior, CAP-7 retains exact Overview and baseline protection, and receipt dismissal is an explicit non-goal.
- (event) Corrected-PRD refresh coherence validation PASS: CAP-1 through CAP-8 remain unique and unrenumbered with complete intent/success pairs; corrected CAP-7/CAP-8 acceptance, receipt-retention constraint, explicit dismissal non-goal, and demonstrable cap behavior remain coherent and lean.
- (note) Corrected-PRD refresh Wrapper-only content: no new correction content was dropped; previously logged PRD ceremony remains wrapper-only, and every retention, baseline, Overview, retry, observability, and non-goal correction is rendered or carried by a declared companion.
- (event) Corrected-PRD refresh preservation validation PASS: receipt dismissal is explicitly deferred, succeeded receipts age out, unresolved receipts are never auto-deleted or compacted, prospective count/byte gates refuse before receipt creation with bounded not-captured list/retry guidance, admission resumes below both caps, retention pressure is not a state, direct retry reuses one receipt, pre-dirty and manual-ref limits are exact, and OverviewDescriptor freshness remains exact.
- (event) Spec refresh finalized after corrected PRD with CAP-1 through CAP-8 unchanged, final Architecture Spine retained, six assumptions unchanged, zero open questions, all requested semantics preserved, and diff check passing.
- (event by spec-update) Headless update resumed from the existing memlog after the finalized Architecture Spine retention update; CAP-1 through CAP-8 remain stable and no new companion is required.
- (decision by spec-update) The current finalized Architecture Spine and its closed update resolution are adopted without exception: SessionEnd deduplicates, applies receiptless baseline eligibility with equality expiry, serializes the real candidate, proves state integrity, then evaluates prospective count and byte admission under one lock.
- (decision by spec-update) Actual cap refusal creates one bounded RetentionRefusalV1 keyed only by hashed session_key and carrying the real candidate byte cost; replay before baseline-created grace expiry re-evaluates the actual candidate, admission removes the marker, and marker replacement never extends grace.
- (decision by spec-update) Status and audit are read-only: state-integrity takes precedence and exposes nullable exact totals with lower bounds and safe evidence; retention-pressure exists only while the current exact predicate fails, while a now-fitting active marker is informational capture-refused-history.
- (constraint by spec-update) Only the exact lowercase-hex hashed session_key is persisted as session provenance. One operator-authorized direct retry of a failed or retry-exhausted receipt reuses that same receipt for exactly one attempt; failure returns it to retry-exhausted without an automatic loop.
- (change by spec-update) The acceptance companion will remove its temporary architecture-supersession caveat because the finalized Architecture Spine now aligns with the corrected PRD; dismissal remains deferred and unresolved receipt evidence remains protected.
- (event by spec-update) Coherence validation PASS: canonical SPEC has the five required fields, exactly CAP-1 through CAP-8 with eight intent/success pairs, all three companions resolve, finalized retention predicates are explicit, and no open-question or stale-supersession language remains.
- (event by spec-update) Preservation validation PASS: CAP IDs remain CAP-1 through CAP-8; the seven prior architecture-bound contract groups and finalized PRD dismissal, unresolved-retention, pre-dirty baseline, manual Git-ref, and Overview freshness protections remain intact; final retention update coverage is complete with zero open questions.
- (event by spec-update) Scoped diff-check PASS: the workspace contains only .memlog.md, SPEC.md, and acceptance-contract.md; direct untracked-file whitespace checks are clean, and no architecture, PRD, epics, readiness, story, or implementation artifact was edited by this refresh.
- (event by spec-update) Final headless bmad-spec refresh finalized with coherence PASS, preservation PASS, stable CAP-1 through CAP-8, adopted final Architecture Spine and update resolution, no unresolved phase blocker, no open question, and story generation disabled.
