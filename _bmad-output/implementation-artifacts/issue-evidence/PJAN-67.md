# Evidence: PJAN-67 — Make MCP mutation fail closed and non-interactive

## Issue
- Ticket: PJAN-67
- Milestone / horizon: adversarial MCP remediation workstream 2 of 5
- Worker: pjan67-implementation-owner (delegated Codex worker)
- Orchestrated by: momo

## Acceptance Criteria
1. Generic recipe execution requires an explicit target and is preview-only unless `apply=true`.
2. Interactive recipes are unavailable through the generic endpoint; Hermes runs only through its dedicated non-interactive tool.
3. Every MCP Hermes path forces quiet mode and captures child output; a raw stdio framing test proves stdout contains only JSON-RPC frames.
4. Hermes deployment defaults to preview; local mutation requires `apply=true`, external effects additionally require `live=true` plus explicit positive opt-ins, `local=false` alone arms nothing, and disabled board authority cannot reach unrelated child processes after shared fleet configuration loads.
5. `skipPlane=true` disables the project board action even when `live=true`, with no provider invocation.
6. Consent and lifecycle failures return `isError=true` before filesystem, provider, systemd, or subprocess effects.

## Repo Changes
- Branch: `fix/PJAN-67-all-mcp-child-env`
- Original base: `90529d2ea750640b0038b4033563a2dca964a0bf`; latest correction base: `74bd6827c967d9bf827ecb9a047174b5d6171004`.
- Implementation head under review: `fbf28feff8baae0e215bdb0c7e0206c6ddfa946f`.
- Pjangler implementation commits include the original `9cba7dc3776959b68c9c8232f4431c3f914df031`, lifecycle correction `dd6f8815693d99b2d7067b2cc07877b56d9ee8d7`, authority/provenance correction `5841e3b5`, outer-boundary correction `1cdb3d9317747babb148de735d365cea3e479d1c`, universal process routing `8d8f892d086e73ca7110e0fa59e1863d20d8a98a`, family-aware environment correction `2fff4413969613aa618b9b98e3aae622a952a128`, isolated fleet-import correction `208058d90498e9dba8d45300be6261b4241046ec`, atomic data-import correction `67f4c402d73c4d51cf8ef67d34fb0903602b9c96`, live-format/provenance correction `3eacb8599f7aa7ed74ca70671e7f260c7ff739f9`, universal fleet-consumer correction `457142d795f44c45308d69ca22df7cbd1da56ac2`, transactional correction `c648f931db0b7617298cc168eeab47ab5c097bf1`, final10 fail-closed correction `74bd6827c967d9bf827ecb9a047174b5d6171004`, and final11 journal/snapshot correction `fbf28feff8baae0e215bdb0c7e0206c6ddfa946f`.
- Generated parity commit: `fbd7ab59`; it names both PJAN-67 and PJAN-72 because the bundles were regenerated at the exact combined head.
- Hermes template commits include `5df96f30e1557fece5772a30a6fca33e03872db9`, authority correction `da8f48b6a9fe3ce644014bcd28859d86b4413424`, child-boundary correction `53a49ce922ee28be1263a7cf099468a09f03b0b0`, family-aware scrub `3555de329639f1e8e33ff8ab1b5fa272d057d682`, isolated fleet-import correction `821baa38e0691f679dcde886804a6ef25c16a094`, assignment-data parser correction `0802782e975389d2b08f8e0bafcb67705e684073`, live-format/writer correction `62730216212e4d2deced0c5622c35ae7155b155f`, universal consumer correction `0201d6fefdc1859e3e30705cfc71d9adaedd6186`, transactional-backfill correction `7beb06dd902fb874d109abc9cf0fb6deced2861b`, final10 correction series `8e0c85022803a2192860ef22b1bf80cf521aab92`, `bb173901c11141db2a61884517c9e52393c24c77`, `09ef30e493eba650713ee7a3c5a2f0ed08423906`, and final11 journal correction `73c78c60de8abe615158fe68ca87bc946acfe89f`. The reviewed parent pins canonical template feature and `origin/main` at exact `73c78c6`.
- Concurrent ancestry disclosure: `9365625d0faa516de44ef33cd60e63491de7c9ea` tracks skill sources and `26b19b9` implements PJAN-72 ticket-provider deep links; neither is PJAN-67 implementation. They were preserved without history rewriting, while PJAN-67 attribution remains isolated to the commits above.
- `src/mcp-server.ts` — strict explicit apply/live/effect consent, dedicated non-interactive Hermes surface, Copier/template/lifecycle preflight, failed-lifecycle MCP errors, safe target-repository identity, and project pre-effect gates.
- `src/lifecycle/preflight.ts` — metadata- and RECORD-bound Copier provenance, vendored-template attestation, rendered-role structural eligibility, executable identity revalidation, and containment checks.
- `src/commands/hermes/ApplyDeferredHostEffects.ts`, `ApplyDeferredExternalEffects.ts`, `EnsureTemplateConfig.ts`, `RunCopierTemplate.ts`, `types.ts` — repo-local render first, host and external tails after eligibility, credential scrubbing, explicit project-root containment, and final deployment metadata.
- `src/recipes/HermesAgentRecipe.ts`, `ProjectRecipe.ts` — eligibility-before-effect sequencing and exactly-once project/Hermes external tails.
- `src/utils/child-environment.ts`, `src/utils/child-process.ts` — centralized sync/async subprocess boundary, native explicit-environment semantics, documented loader/interpreter/shell family filtering, and controlled override preservation.
- `templates/hermes-agent` — pinned to canonical template main `62bd160`; the universal consumer correction is isolated at `0201d6f`, and the pinned union includes disclosed concurrent provisioning changes.
- `tests/pjan-67-regressions.mjs`, `pjan-67-lifecycle-preflight-regressions.*`, `pjan-67-mcp-child-process-regressions.mjs`, `pjan-67-trusted-lifecycle-regressions.mjs` — protocol, provenance, full process-site routing, family-aware environment behavior, real-template, raw-stdio, zero-effect, positive create/sync, containment, and exactly-once coverage.
- `package.json`, `dist/index.js`, `dist/mcp-server.js`, `dist/prompt.js` — full-chain registration and regenerated runtime bundles.
- Migrations / schema: none.

## Verification
- Initial mutation proof: the PJAN-67 discovery assertion failed against the base revision before implementation.
- First independent SPEC review: `/root/pjan67_spec_review`; AC 1–3 passed and AC 4–6 failed. It reproduced the real template ignoring `SKIP_PLANE` and three lifecycle failures occurring after target, config, Copier, or provider effects.
- Second independent SPEC review: `/root/pjan67_spec_recheck`; AC 1–3 and 5 passed, while AC 4 and 6 failed. It proved that shared fleet loading restored disabled board authority in unrelated children and that Copier execution was not bound to the executable attested during preflight.
- Third independent SPEC review: `/root/pjan67_spec_final2`; AC 1–5 passed and AC 6 failed at `fbd7ab5`. It proved that the project-owned Hermes route wrote `.project.json` before outermost Copier identity revalidation and that deferred host/external helpers inherited ambient Python startup configuration.
- Fourth independent SPEC review: `/root/pjan67_spec_final3`; the corrected outer transaction, Copier binding, template post-source scrub, and targeted tails passed at `1cdb3d9`, but AC 6 remained open because BMAD probe/install, lifecycle Git, and systemd helper children still inherited executable ambient startup configuration.
- Fifth independent SPEC review: `/root/pjan67_spec_final4`; the centralized wrapper at `8d8f892` covered all 29 inventoried production process-creation sites, preserved native explicit-environment semantics, killed four bounded mutations, and passed every runnable release gate. AC 6 remained open because GNU loader audit/ABI controls plus Bash exported-function and trace/startup controls survived the fixed-name parent and post-fleet template scrubs.
- Family-aware remediation at parent `2fff441` and template `3555de3` strips the documented GNU loader controls and ABI variants, GLIBC tunables, macOS loader controls, exported Bash functions, and Bash startup/trace controls while preserving controlled paths, consented credentials, explicit minimal environments, and non-control application settings. Implementer typecheck, deterministic build, all four PJAN-67 suites, template pytest, focused cross-ticket suites, full package tests, and publication gates passed; a new independent SPEC pass is in progress.
- Sixth independent SPEC review: `/root/pjan67_spec_final5`; parent sync/async behavior, all inventoried process routing, official gates, and four reversible regression-strength checks passed at `2fff441`/`3555de3`. AC 6 remained open because a fleet configuration can mark a startup-control variable readonly before the template's post-source scrub, leaving it exported to descendants. The delegated correction now isolates fleet import from the provisioning shell rather than relying on later mutation.
- Seventh independent SPEC review: `/root/pjan67_spec_final6`; exact parent `208058d`, canonical template `2addc6c`, isolated template attribution `821baa3`, and concurrent template ancestry were verified. AC 6 remained open because post-configuration helper command resolution can be redefined by the sourced file and because parent records are applied before complete child-status, framing, duplicate, and stream-termination validation, permitting partial import on a later invalid record.
- Atomic data-import remediation at parent `67f4c40`, isolated template `0802782`, and canonical template `2483525` never sources fleet configuration. It parses the documented assignment-only grammar in isolated Python, validates the complete NUL-framed result and child status before import, stages all values, then applies with rollback. Implementer red-to-green, 111 template tests, real rendered Copier/MCP behavior, focused cross-ticket suites, typecheck/build parity, full package/prepublish, secret, lock, and exact-tip submodule gates passed; a new independent SPEC pass is in progress.
- Eighth independent SPEC review: `/root/pjan67_spec_final7`; exact identities, canonical template union, parser atomicity, five reversible mutations, and the full release matrix passed at `67f4c40`/`2483525`. AC 6 remained open because the current live fleet data uses a safe legacy variable expansion rejected by the parser; the fleet writer emits raw unquoted values and fails to recognize optional-export/indented keys; the executed parser helper is absent from both canonical-template and rendered-template provenance sets; and a lone-surrogate ANSI-C value escapes normalized diagnostics. A missing or tampered helper passed both preflights in a real normalized render.
- Live-format/provenance remediation at parent `3eacb85`, isolated template `6273021`, and canonical template `a39bc96` supports only the exact legacy fleet-home reference, canonically and atomically serializes writer values, normalizes encoding diagnostics, and byte-attests the parser helper in canonical and rendered preflights before effects. Implementer live-shape, 125 template tests, real MCP zero-effect/provenance, focused/full/prepublish, typecheck/build parity, secret, lock, and exact-tip submodule gates passed; a new independent SPEC pass is in progress.
- Ninth independent SPEC review: `/root/pjan67_spec_final8`; exact identities and the main parser/writer/provenance fixes passed at `3eacb85`/`a39bc96`. AC 6 remained open because the generated launcher plus fleet-sync, migrate-unify, and backfill scripts still source fleet data as executable Bash, backfill emits legacy source-based snippets, the compatibility parser accepts a literal prefix or repeated allowed home token beyond the locked one-leading-token grammar, and the writer's path check followed by replacement can overwrite a concurrent replacement instead of rejecting the race.
- Universal consumer remediation at parent `457142d`, isolated template `0201d6f`, and canonical template `62bd160` routes every tracked/rendered/generated/maintenance fleet consumer through one attested data loader, forbids executable sourcing, uses the canonical backfill writer, tightens legacy grammar, and performs race-detecting atomic exchange with fail-closed portability behavior. Implementer 144 template tests, direct/MCP provenance, focused/full/prepublish, typecheck/build parity, secret, lock, and exact-tip submodule gates passed; a new independent SPEC pass is in progress.
- Tenth independent SPEC review: `/root/pjan67_spec_final9`; exact identities, 144 template tests, the full parent release matrix, and 12 of 16 mutations passed at `457142d`/`62bd160`. AC 6 remained open because backfill mutates before complete validation and lacks batch rollback; failed reverse exchange can delete displaced data; canonical helper attestation trusts mutable vendored bytes; source gates miss semantic indirection and executable suffixes; canonical/rendered heartbeat and repair workflows omit loader migration; operator guidance still recommends sourcing; writer metadata preservation orders chmod before chown and ignores ownership failure; and npm packaging gates do not require the loader/parser assets.
- Eleventh independent SPEC review: `/root/pjan67_spec_final10`; exact parent `c648f931`, template `e5b1966`, 152 template tests, all four PJAN-67 suites, the full package/prepublish matrix, 25 official mutations, and 13 additional harmless probes completed. The verdict remained ISSUES: same-inode concurrent updates can be lost; backfill strips special mode bits and accepts ownership drift; executable suffix variants escape inventory; symlink ancestors escape lexical containment; the declared Git object does not bind all executed Copier tasks; fresh/backfill systemd values are not safely encoded; heartbeat units are omitted from backfill reconciliation; multi-file commit lacks durable crash recovery; snapshotted parser bytes are reopened by mutable path; and npm package verification covers only three runtime assets.
- Final10 remediation at parent `74bd6827`, template `09ef30e`, and the exact 48-asset Git-object manifest implements content/metadata CAS, exact file/directory metadata, executable inventory, physical containment with stable parent dirfds, an immutable private Copier snapshot, context-specific systemd serialization, heartbeat reconciliation, fsynced and locked crash recovery, captured-byte parser execution, and complete package byte/mode checks. Implementer template 174 tests, crash/concurrency fixtures, focused cross-ticket suites, typecheck/build parity, full package tests, and prepublish gates passed; independent SPEC final11 is in progress.
- Twelfth independent SPEC review: `/root/pjan67_spec_final11`; exact parent `74bd6827`, template `09ef30e`, 174 template tests, all official parent/release gates, and 20 independent harmless probes completed before the reporting channel was classifier-interrupted. The verdict is ISSUES from three reproduced High transaction defects: prepared directories/files can become durable before any discoverable journal exists; journal post-link fsync failure can leave an orphan stage link; and a parent rename after exchange can make commit recording fail, remove rollback handles/journal, and lose the original bytes. Same-user writability of the private Copier snapshot remains an explicit regression hypothesis for the correction owner to settle before the next review.
- Final11 remediation at parent `fbf28fe`, template `73c78c6`, and the content-addressed private Git handoff publishes durable `preparing` intent before staging, cleans and fsyncs journal aliases, records mutation ownership before fallible checks, restores through retained dirfds, and makes same-user worktree rewrites irrelevant to exact-ref Copier execution. Implementer template 179 tests, 14 transaction tests, focused cross-ticket suites, typecheck/build parity, full package tests, and prepublish gates passed; independent SPEC final12 is in progress.
- Thirteenth independent SPEC review: `/root/pjan67_spec_final12`; exact parent `fbf28fe`, template `73c78c6`, 179 template tests, all official parent/release gates, 23 independent passing transaction cases, and four bounded targeted fixtures completed. The verdict is ISSUES from three reproduced High defects: a named journal stage can survive before canonical publication and later poison valid recovery; `read_regular_file()` can block on a FIFO before type validation; and the serialized CAS omits captured ctime while replacement loses ordinary user extended attributes.
- Remediation proof: the exact project-init, bootstrap, and dedicated-Hermes reviewer fixtures now return `isError=true` before target/config/registry/Copier/provider/systemd effects.
- Existing malformed Hermes lifecycle state is rejected before a new role render, host config change, fleet-registry write, child process, or provider call.
- Real-template `SKIP_PLANE` tests prove two no-authority paths make zero provider calls and one positively granted path calls the provider once.
- Trusted-Copier integration proves CommonProject create, existing-project sync without a Copier rerun, dedicated Hermes all-grants, and project-owned all-grants.
- Dedicated and project-owned live tails dispatch each selected external script once, invoke board creation once, run only after eligibility, and persist correct deployment metadata.
- A disposable enclosing Git-repo sentinel proves fresh-target scripts cannot climb into and mutate a parent checkout manifest.
- Raw newline-framed stdio uses the attested installed Copier and real vendored template; child stdout/stderr remain captured, MCP stdout contains JSON-RPC frames only, and provider credentials are absent from ungranted child environments.
- Real-render correction coverage exercises dedicated Hermes, bootstrap, and project-init with disabled board authority plus selected runtime/systemd tails; provider aliases remain absent from every child, provider calls remain zero, and the positively granted board path calls exactly once.
- Copier correction coverage accepts the installed UV Copier 9.14.0 identity; rejects text-only pip-user, pipx, pyenv, Homebrew, and first-PATH lookalikes; detects post-preflight launcher replacement; invokes the exact canonical executable; and proves zero target/config/registry/provider/systemd/spoof effects on rejection.
- `pytest -q` in `templates/hermes-agent` — 179 passed at candidate `73c78c6`.
- `npm run typecheck` — passed.
- `npm run build` and generated-bundle parity — passed.
- `node tests/pjan-67-lifecycle-preflight-regressions.mjs` — passed.
- `node tests/pjan-67-regressions.mjs` — passed.
- `node tests/pjan-67-trusted-lifecycle-regressions.mjs` — passed with the installed UV Copier.
- MCP catalog/server, project-registry, fleet-shared Bloodbank, PJAN-57 lifecycle/generated-project, PJAN-71, and Momo lifecycle focused regressions — passed.
- `npm test` full chain — exited 0 on the final implementation tree.
- `npm run check:lock`, `npm run check:tracked-secrets`, and `npm run check:submodules -- --remote --recursive --archive --npm` — passed.
- `git diff --check` — passed in parent and template repositories.
- Parent feature `origin/fix/PJAN-67-all-mcp-child-env` is published at candidate head `fbf28fe`; parent `origin/main` remains independently at `cd8c1e4`. Template feature and canonical template `origin/main` are both published at `73c78c6`, and the parent gitlink matches that exact tip.
- The isolated worker hand-back schema validation is `VALID` at candidate head `fbf28fe`; the canonical shared handback must be refreshed after independent acceptance and integration.
- `_bmad-output/implementation-artifacts/diffs/PJAN-67.diff` remains a stale pointer until the accepted integration head is known; it must be regenerated before close.

## Ledger Update
- Bloodbank decision/events emitted: `2aa5c170-2e71-47b3-b15b-2544506aa43c`, review-reopen decision `0bd9e6d8-a054-406b-b5ba-2fd10886f3e7`, final-head hold decision `51a1e0dc-e2c6-4d23-8d8b-5916cf4293a1`, universal child-boundary decision `0ba5ff7f-0568-4603-805e-89eb9112f4c3`, fifth-SPEC hold decision `2b059ad1-a886-4285-989f-76ce779ca971`, sixth-SPEC hold decision `58750af1-7ee7-442b-885a-4e41d1175857`, concurrent template-publication decision `5e149ede-c31c-45fb-93ea-26f9229c9158`, exact-main-pin decision `f2d52218-85cc-41f4-8f68-d0b2c46d2b0d`, seventh-SPEC hold decision `c70ee727-d654-4572-908c-1d7a616413a2`, assignment-only fleet contract decision `920ace8e-e6f1-47f9-bacb-92568e14351b`, eighth-SPEC hold decision `9d35aab9-be16-4e2c-9558-56c791adbc0b`, ninth-SPEC hold decision `fedac8bf-082e-4d43-a534-0e32edc157c2`, tenth-SPEC hold decision `720b5b34-a0b3-4dc9-aeac-124a729ffdf4`, final10 hold decision `a7be6392-df0c-4042-9593-320ea2d21569`, final11 hold decision `5a65652e-26b7-47ad-beca-2ce072b21494`, and final12 hold decision `bb84d66b-4076-49fa-920e-599b2482469f`.
- Ledger updated: yes

## Known Gaps
- The final11 defects have an immutable published correction at `fbf28fe`/`73c78c6`, but final12 reproduced three remaining file-transaction defects: journal-alias reconciliation, bounded non-regular input rejection, and complete CAS/metadata preservation. A new correction plus a brand-new SPEC pass and subsequent quality review are required before acceptance.

## Close Recommendation
- Close recommendation: hold
- Rationale: the final11 defects have a green candidate, but final12 still proved three High lossless-file-transaction gaps. Momo is holding acceptance until correction plus fresh independent SPEC, quality, and integration reviews pass.
