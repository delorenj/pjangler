---
topic: Project Notebook
updated: 2026-08-19T14:33
---

- (decision by user) Every PJangler-bootstrapped repository receives one Companion Notebook whose display name deterministically follows the repository name; this is the core user outcome.
- (decision by user) Project Notebook is a modular PJangler lifecycle component with high-level note CRUD and search under pj notebook.
- (decision by user) The global project-notebook skill carries project-scoped hooks projected through the agent master hook fanout.
- (decision by user) Session start primes agent context from a bounded Overview Note alongside Hindsight recall.
- (decision by user) Session end captures changed documentation and a concise session summary using a low-cost LLM when available.
- (event by Codex) Headless Update mode selected for PJAN-77; the existing partial PRD is being reconciled into the consolidated Essential Spine.
- (decision by Codex) This is a chain-top brownfield internal developer-tool PRD that must support architecture, epics, stories, and implementation readiness.
- (decision by Codex) A Notebook Binding has a stable service identifier plus a deterministic display name so repository renames can be repaired without creating duplicates.
- (decision by Codex) Global defaults and per-project bindings belong in projects.yaml; repository policy overrides belong in .project.json; credentials never belong in either.
- (decision by Codex) PJangler plan and apply safety stays intact: dry-run performs no writes, local configuration is applied explicitly, and remote Notebook Service mutation requires the existing live-action gate.
- (decision by Codex) The v1 public CLI uses pj notebook for status, ensure, note CRUD, search, overview, audit, and migrate with human and versioned JSON output.
- (decision by Codex) All note search and mutations are constrained to the current repository Notebook Binding even if the upstream service exposes global search.
- (decision by Codex) The Overview Note is addressed by stable note identifier, fetched at most once per session, bounded before prompt injection, and fail-open.
- (decision by Codex) Session Capture records the session boundary quickly, processes work off the closing path, detects eligible changed documentation from the repository diff, and deduplicates retried close events.
- (decision by Codex) Version-controlled repository documents remain authoritative; notebook copies are searchable derivatives and must carry provenance.
- (decision by Codex) Notebook audit and migrate are module-scoped, dry-run capable, idempotent, and preserve unrelated manifest, registry, and hook content.
- (decision by Codex) Notebook Service endpoints are configured by hostname; authentication resolves at runtime from environment or secret-manager references and values are never persisted or printed.
- (decision by Codex) The CLI JSON contract starts at schema version 1; additive changes are backward-compatible, breaking changes require a major PJangler version, and deprecated forms retain a compatibility window.
- (decision by Codex) MVP excludes fleet-wide bulk migration, cross-project search, Notebook Service administration, rich media ingestion, UI work, and new MCP tools.
- (assumption by Codex) The verified Open Notebook v1.14 deployment keeps the current notes CRUD surface and does not add a notebook-scoped search filter before implementation.
- (assumption by Codex) Supported agent clients expose a true session-start and session-close boundary through the existing fanout; turn-level stop events are not treated as session close.
- (assumption by Codex) A configured low-cost LLM may be unavailable, so deterministic summary generation is a required fallback rather than a degraded optional behavior.
- (assumption by Codex) The first release serves a trusted solo operator and one shared Notebook Service; multi-tenant authorization is outside MVP.
- (event by Codex) Initial source extraction completed across the user attachment, brownfield documentation, live code reconnaissance, PJAN-77, and Notebook Service reconnaissance.
- (decision by Codex) Every supported Project Registry backend must round-trip Notebook Binding fields; YAML-only persistence is not an acceptable split contract.
- (decision by Codex) Eligible Document ingestion uses repository-relative path plus content digest as derivative identity; unchanged content is a no-op and updated content replaces the prior derivative without deleting source history.
- (decision by Codex) The Notebook Service has no built-in endpoint default in v1 because the verified public hostname is interactive-access protected; operators must configure an automation-reachable hostname explicitly.
- (decision by Codex) The canonical user-facing grammar is pj notebook status or create, direct note CRUD and search verbs with note or notes operands, overview, audit, and migrate; hook entrypoints are managed internal subcommands.
- (assumption by Codex) Initial performance budgets are two seconds for bounded session-start priming and 250 milliseconds for foreground session-close enqueue; architecture may tighten them with measurement evidence.
- (decision by Codex) MVP Overview Note scaffolding contains project identity, purpose placeholder, and links to authoritative repository documents; generation or synthesis of richer overview content is deferred.
- (decision by Codex) Notebook Service content may persist until explicit deletion, while Eligible Document derivatives update in place by provenance identity and Session Capture summaries remain append-only by session identity.
- (change by Codex) Reconciled stale source conflicts in favor of live brownfield architecture: Node 20 plus TypeScript and Commander, singleton RecipeRegistry ownership, recipe-scoped parity, and current plan/apply semantics.
- (change by Codex) Replaced the partial classic draft with the consolidated Essential Spine: purpose, thesis, users and journeys, glossary, 21 stable Functional Requirements, explicit non-goals and MVP, metrics, public contract, cross-cutting NFRs, risks, open items, and assumptions.
- (change by Codex) Created addendum.md to preserve configuration, binding lifecycle, CLI and JSON schema, adapter, Managed Hook, Session Capture, verification, and options-considered mechanisms outside the PRD narrative.
- (override by Codex) The later canonical CLI decision supersedes the earlier provisional ensure verb: v1 user-facing lifecycle command is pj notebook create with idempotent ensure semantics.
- (change by Codex) Wrote one reconciliation artifact for each listed source plus live code, PJAN-77, and Notebook Service reconnaissance; all input signals now trace to PRD or addendum and no blocking gap remains.
- (event by Codex) Memlog audit completed: every decision and assumption is represented in prd.md or addendum.md; source and workflow events are represented by reconciliation artifacts.
- (decision by Codex) Claude Code SessionStart and SessionEnd are the minimum non-empty Managed Hook client contract for MVP; additional clients require proven true lifecycle boundaries and Codex turn-level Stop does not qualify.
- (decision by Codex) Remote authorization is operation-class specific: explicit read command authorizes a read, direct mutation command authorizes that mutation, init/create/migrate remote work requires --live, and autonomous hooks require durable per-project opt-in shown in the Apply plan.
- (decision by Codex) Session start records a repository baseline independently of overview fetch; missing baseline blocks Eligible Document upload, produces an incomplete truthful receipt, and remains recoverable through capture status/retry with an explicit baseline.
- (decision by Codex) Capture receipts expose queued, processing, succeeded, failed, retry-exhausted, and blocked-missing-baseline outcomes; retry is bounded and unresolved outcomes remain visible until recovered or dismissed.
- (decision by Codex) Overview replacement is an in-place content update that preserves the designated note identifier; generic deletion of the designated Overview Note is blocked.
- (decision by Codex) Project Notebook checks participate in ordinary pj audit and the final init audit; unauthorized remote checks are skip, while live-authorized init requires remote postconditions before registry persistence.
- (decision by Codex) The Project Registry owns notebook_id, notebook_name, overview_note_id, and binding state; the Project Manifest carries a read-only projection of those fields plus repository policy overrides, and registry authority wins on drift.
- (decision by Codex) MVP supports the YAML Project Registry including PJ_PROJECT_REGISTRY path overrides and the existing PostgreSQL RegistryStore projection; both require additive migration and unknown-field preservation.
- (decision by Codex) Persisted Notebook Binding states are disabled, planned, and linked; observed status also reports unconfigured, healthy, drifted, unavailable, and blocked.
- (assumption by Codex) Capture-quality acceptance samples at least 20 staged sessions and requires 95 percent factual claim support plus complete provenance; the threshold may be tightened after staged evidence.
- (change by Codex) Accepted all four high and all material medium and low rubric findings for correction before finalization.
- (change by Codex) Resolved the rubric gate: defined the minimum Claude Code lifecycle client, remote authorization matrix, complete Capture Receipt recovery, Overview identity, global audit participation, exact binding ownership, closed state model, YAML and PostgreSQL registry support, and capture-quality metric.
- (event by Codex) Applied bmad-review structure and prose standards using the Strategic Context pyramid: preserved the Essential Spine, removed inconsistent wording, normalized glossary terms, and kept technical mechanisms in addendum.md.
- (event by Codex) Open-item triage completed with no phase-blocking question; six explicit assumptions remain indexed for downstream confirmation.
- (event by Codex) PRD finalized
- (override) The v1 receipt-retention decision supersedes the earlier dismissal clause: receipt dismissal is deferred pending separate UX and safety design; unresolved receipts remain visible until recovered and are never automatically deleted or silently compacted.
- (change) Resolved readiness MAJOR-1 without changing FR IDs: succeeded receipts expire under bounded policy; configured unresolved count and byte caps gate new SessionEnd capture before receipt creation, fail open with a bounded retention-pressure finding and exact list/retry recovery action, and reopen only after existing unresolved work is recovered below both caps.
- (change) Resolved the only rubric medium tied to MAJOR-1: direct capture retry now authorizes one additional attempt on the same failed or retry-exhausted receipt after operator correction, returns failure to retry-exhausted, requires an explicit baseline when missing, and never creates a second receipt or automatic loop.
- (event) Memlog audit completed for the MAJOR-1 update: the prior dismissal decision is explicitly superseded; the PRD, addendum, and readiness reconciliation contain the adopted v1 retention and recovery contract.
- (event) Applied bmad-review structure and prose standards to the MAJOR-1 update: preserved repeated scope clauses needed for downstream extraction, split the dense retention flow, and normalized count, byte-usage, retry, and second-receipt wording without changing product scope.
- (event) PRD finalized after receipt-retention update and reviewer recheck
- (change) Resolved the two MAJOR-1-related rubric lows: FR-5 now names blocked-missing-baseline exactly so no seventh state is implied, and reconciliation distinguishes preservation of admitted unresolved evidence from the explicitly uncaptured session at the admission gate.
- (event) PRD finalized
- (change) Bound the narrow v1 Overview freshness invariant: each write records ordered authoritative references and revision/content identities; SessionStart recomputes after baseline and labels PROJECT NOTEBOOK OVERVIEW DRIFT; notebook.overview-note audit/migrate owns detection and same-ID repair.
- (change) Bound session attribution against pre-existing dirty documentation: SessionStart captures HEAD plus bounded tracked-document status and per-file digests before Overview work; close compares that state; manual baseline refs prove committed comparison only and block or exclude untrustworthy uncommitted provenance.
- (change) Closed the two editorial notation findings by correcting the Assumptions Index reference to section 13 and normalizing Manifest policy fields to session_start_enabled and session_capture_enabled.
- (event) Accepted all four residual rubric findings for scoped correction and queued a clean independent reviewer rerun.
- (event) Independent final PRD reviewer gate passed all seven dimensions strong with zero critical, high, medium, or low findings; MAJOR-1 and FR-1 through FR-21 stability checks passed.
- (event) PRD finalized
