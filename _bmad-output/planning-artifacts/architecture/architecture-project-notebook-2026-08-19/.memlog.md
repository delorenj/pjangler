---
scope: PJAN-77 companion project notebooks across PJangler init, registry, CLI, audit/migrate, and truthful session hooks
purpose: build-substrate
altitude: feature
updated: 2026-08-19T15:25
---

- (event) Headless create run activated with caller-bound altitude feature, purpose build-substrate, and explicit workspace; no additional presentation artifact is required.
- (constraint) SPEC-project-notebook CAP-1 through CAP-8 and companions acceptance-contract.md and PRD addendum.md are binding; the finalized PRD is traceability input and current code wins over stale prose.
- (constraint) The configured persistent project-context.md source is absent; brownfield reality was checked directly in current source, tests, package metadata, live hook configuration, and the live Open Notebook container.
- (version) Brownfield PJangler evidence at git HEAD fe17d1fb2a8296a749ced084c3fe3f699cecadb3 on feat/PJAN-77-project-notebook: package 1.3.3, ESM, engines Node >=20; executing Node is v26.5.0.
- (version) Installed package evidence: commander 14.0.3, pg 8.22.0, yaml 2.9.0, zod 4.4.3, TypeScript 5.9.3, esbuild 0.25.12, node-pg-migrate 8.0.4.
- (version) Live service evidence: Open Notebook /api/config reports 1.14.0 online; container is pinned to lfnovo/open_notebook:v1-latest digest e53f90d6, with the local host API bound at http://127.0.0.1:8502.
- (version) Open Notebook 1.14.0 source and live probes confirm notebook names are non-unique, create has no caller idempotency key, note responses omit notebook ownership, GET /api/notes accepts notebook_id, and POST /api/search is global with limit at most 1000 and no notebook filter.
- (version) Current official Claude Code hook documentation verified 2026-08-19: SessionStart begins or resumes a session and injects stdout context; Stop ends one agentic response; SessionEnd terminates the session, cannot block, and has a 1.5 second default overall budget that can be raised by per-hook timeout up to 60 seconds.
- (decision) AD-1 selects a hexagonal lifecycle-module paradigm. One singleton NotebookRecipe and its application core own behavior; Commander, ProjectRecipe, hook wrappers, and the worker are inbound adapters, and Project Registry, Git, receipt state, summarizer, and Open Notebook are outbound ports. This prevents CLI, hook, and migration implementations from diverging.
- (decision) AD-2 keeps NotebookRecipe registered before ProjectRecipe and adds notebook as a truthful ProjectRecipe dependency. Dependency init handles only local owned readiness/projection; ProjectRecipe remains the transaction orchestrator and executes notebook remote actions in its explicit external tail. This prevents nested transactions and remote writes during dry-run or local eligibility work.
- (decision) AD-3 extends the pure ProjectInitPlan with notebook local and live actions. Apply writes scaffold and planned local projection, closes local checks, initializes Git, reconciles remote notebook and Overview only when --live, verifies remote postconditions, folds returned IDs into the manifest, and persists Registry last. Recoverable external failure preserves planned state instead of deleting the new repo; rollback deletes a fresh target only for failures before the external boundary.
- (decision) AD-4 assigns ownership: Git owns source documents; Registry owns global notebook config and per-project state/notebook_id/notebook_name/overview_note_id; Manifest owns policy and mirrors binding read-only; Open Notebook owns derivative note bodies; XDG state owns session baselines and receipts. Registry wins binding drift; migration projects Registry to Manifest.
- (decision) AD-5 requires lossless owned-path updates. YAML and JSON parsers preserve unknown top-level, project, manifest, hook, and notebook-policy fields; project re-init starts from existing records rather than reconstructing only known keys. PostgreSQL gains additive projects.notebook JSONB plus a registry-settings JSONB row/table for global notebook config and a partial unique stable-ID index.
- (decision) AD-6 defines binding reconciliation by stable service IDs plus a deterministic pjangler project marker in the notebook description, never display name alone. Zero marker matches permits one POST; one is adopted; more than one is CONFLICT. After ambiguous create, bounded reconciliation runs and no second POST occurs in that invocation.
- (decision) AD-7 uses a typed, bounded fetch adapter with runtime Zod validation, explicit AbortSignal deadlines, response and collection caps, no unsafe automatic write retry, and normalized symbolic errors. The base URL has no built-in default; HTTPS hostnames or explicit loopback HTTP are allowed, embedded credentials and hardcoded LAN addresses are rejected.
- (decision) AD-8 proves membership before disclosure or mutation by obtaining the bound notebook note set from GET /api/notes?notebook_id and locating the stable note ID there. Single-note GET/PUT/DELETE is never used as ownership proof. MVP text search runs locally over that scoped bounded note set; global upstream search and semantic search are not used because post-filtering at the upstream 1000-result ceiling cannot prove completeness.
- (decision) AD-9 stores Overview, Eligible Document derivatives, and Session Captures as human notes with a bounded PjanglerNoteEnvelopeV1 marker containing base64url canonical JSON identity/provenance. Marker reconciliation yields zero=create, one=update/adopt, many=CONFLICT; generic deletion rejects overview_note_id. Native Open Notebook Source ingestion is deferred to avoid two competing derivative models.
- (decision) AD-10 fixes the pj notebook CLI grammar from the adopted addendum and a schema_version 1 envelope. JSON stdout contains only the envelope; diagnostics use stderr. Numeric exits group symbolic errors: 0 success, 2 invalid input, 3 config/auth, 4 domain conflict/not-found/isolation/drift, 5 retryable remote, 6 protocol/internal, with 1 reserved for legacy/unclassified.
- (decision) AD-11 keeps synchronous RecipeCheck ownership by passing an optional immutable NotebookObservation in LifecycleContext. pj notebook audit and live ProjectRecipe postconditions prefetch the observation asynchronously; ordinary pj audit supplies none so remote rules report skip, never pass. Fresh init also supplies its in-memory NotebookPlan so checks do not require a registry entry that is intentionally not persisted yet.
- (decision) AD-12 makes pj notebook migrate a NotebookRecipe-owned orchestration surface: it selects only notebook public rule IDs, plans local versus live actions, invokes no migrateAll, requires --apply for writes and --live as an additional gate for remote writes, and verifies postconditions. Generic migration of a remote notebook rule blocks with the exact notebook command rather than performing hidden network work.
- (decision) AD-13 chooses one canonical hook package: /home/delorenj/code/skillex/all-skills/project-notebook owns hooks.master.json, wrappers, and the surgical projector; ~/.agents/skills/project-notebook is only its symlinked projection; PJangler owns audit/invocation; ~/.claude/settings.json is generated live state. The projector owns only commands carrying marker project-notebook.v1, preserves foreign entries and order, and is byte-idempotent.
- (decision) AD-14 maps Project Notebook only to Claude Code native SessionStart and SessionEnd in MVP. It never maps Stop and never consumes Bloodbank session_end semantics. The existing Bloodbank master and CommonProject masters remain foreign ownership; their Stop mappings are not copied, and their surgical fanout must preserve the project-notebook.v1 entries. Unsupported clients report skip rather than lifecycle equivalence.
- (decision) AD-15 makes session start record a trustworthy baseline before any overview decision. The baseline contains HEAD plus bounded tracked-document path digests and status evidence, not bodies; one atomic per-session claim permits at most one Overview emission. Start emits bounded clearly labeled Notebook Service context or a bounded fail-open diagnostic within the two-second p95 budget.
- (decision) AD-16 stores mutable state under $XDG_STATE_HOME/pjangler/notebook/v1 or ~/.local/state/pjangler/notebook/v1, keyed by a hash of canonical project identity. Directories are 0700 and files 0600; create uses exclusive open and updates use same-directory temp, fsync, atomic rename. Repositories receive no runtime files.
- (decision) AD-17 derives receipt identity from project, client, session, and capture kind. SessionEnd validates local identity, atomically creates or reuses one queued receipt, spawns the Node worker detached without a shell or payload/credential argv, and returns. Worker leases make queued or stale-processing receipts restartable; states are queued, processing, succeeded, failed, retry-exhausted, and blocked-missing-baseline.
- (decision) AD-18 computes changed eligible documents by comparing the start digest map and Git baseline to bounded close evidence. Only repository-contained, regular, version-controlled documentation matching policy is read; ignored, generated, binary, symlink-escaping, oversized, secret-like, and over-budget inputs are excluded with reason codes. Derivatives carry project, path, revision or digest, session, capture time, and policy version.
- (decision) AD-19 supplies the optional summarizer as a configured argv-based port executed without a shell, with finite input/output/time bounds and no stored secret. Its input is already eligible and redacted. Failure or absence produces the deterministic fallback sections: changed documents, other changed path names, verification evidence, unresolved work, and evidence-insufficiency.
- (decision) AD-20 applies the authorization matrix literally: explicit reads authorize only reads unless --local-only; a direct add/update/delete/overview command authorizes only that mutation and delete needs confirmation; init/create/migrate remote mutation needs --live; hook remote behavior needs durable manifest opt-in. Explicit disable dominates every hook setting.
- (decision) AD-21 uses incremental rollout and non-destructive rollback: ship additive schemas and fake-service tests, project/check the global skill hooks, dogfood one explicit loopback binding, then migrate repositories individually. Disable policy and remove only marker-owned live hooks to roll back; leave remote notebooks and stable bindings intact because notebook deletion and fleet migration are non-goals.
- (decision) AD-22 makes verification evidence part of the architecture contract: isolated HOME/XDG/registry, fake service and optional disposable PostgreSQL, generated and packed CLI, contamination and ambiguous-create fixtures, hook event/config preservation tests, receipt race/restart tests, secret/path adversaries, second-run byte/remote no-op proof, and p95 start/close measurements. Tests never touch operator registry or live notebooks.
- (assumption) A-1 adopted: Overview defaults to 4000 characters and implementation defines a separate hard ceiling; the exact hard ceiling remains code-owned but must be tested.
- (assumption) A-2 adopted: staged capture acceptance uses at least 20 sessions, complete provenance, and at least 95 percent factual claim support.
- (assumption) A-3 adopted: a low-cost summarizer is normally available, while deterministic fallback is mandatory and equivalent for delivery continuity.
- (assumption) A-4 adopted: Open Notebook 1.14 behavior remains compatible; contract fixtures and runtime validation, not the image tag, decide compatibility.
- (assumption) A-5 adopted: MVP is one trusted operator and one shared service; per-user and multi-tenant authorization are deferred.
- (assumption) A-6 adopted: session-start p95 is at most two seconds and session-close foreground enqueue p95 is at most 250 milliseconds on the target workstation.
- (constraint) No credential value may enter config, logs, JSON, exceptions, hook payloads, state files, tests, or notes. Only an auth environment-variable name or secret reference is persisted; the adapter resolves the value at runtime and never echoes it.
- (constraint) No new daemon, UI, MCP command, fleet-wide migration, cross-project search, Open Notebook administration, native Source ingestion, rich media, transcript upload, or destructive remote deletion is part of MVP.
- (event) Architecture source reconciliation completed across canonical spec companions, finalized PRD/addendum, PJangler code/tests/package metadata, live Open Notebook 1.14 routes, installed global hook topology, and current official Claude Code lifecycle documentation.
- (event) Distilled AD-1 through AD-22, conventions, structural seed, transaction, state machines, verification, rollout, and deferred boundaries into ARCHITECTURE-SPINE.md.
- (decision) Hook coexistence boundary: canonical project-notebook skill source and marker-owned inner-hook projector remain independent of Bloodbank; verified current Bloodbank merge preserves foreign inner hooks, and release requires a project-notebook.v1 coexistence regression.
- (decision) Ambiguous create safety: once a request may have been dispatched, automatic flows reconcile only and remain planned/blocked if unresolved; they never send a second blind POST because Open Notebook v1.14 exposes no idempotency key.
- (event) Independent rubric review completed READY-WITH-FIXES: RW-1 through RW-5 identified durable create journal, brownfield sync/rollback, Overview Drift, baseline policy, and user-note identity gaps.
- (event) Independent current-tech/current-code review completed NOT-READY: CT-1 through CT-5 identified unified registry-last tail blocker plus rollback, packed skill distribution, auth probe, and Bloodbank assertion corrections.
- (event) Independent adversarial divergence review completed READY-WITH-FIXES: external commit/journal, note/session identity, lossless config/hook representation, and JSON/search/error ambiguity were load-bearing.
- (decision) Review reconciliation adopted one vocabulary: RemoteMutationJournalV1, user-note, exact sha256 session_key, fixed-order ProjectExternalEffectPlan, anchored PJ_HOOK_OWNER=project-notebook.v1 command prefix, and OverviewDescriptorV1.
- (change) Resolved RW-1..5, CT-1..5, and adversarial 1..4 in the spine; added AD-23 packed skill export, transaction/failure bindings, journal/session state, Overview Drift, canonical hooks, per-command JSON, deterministic search, and review-resolution evidence.
- (change) Adversarial final recheck found one hash-framing ambiguity; AD-17 now fixes exact UTF-8 NUL-delimited receipt and capture ID bytes. Recheck then passed 4/4 with zero residuals.
- (event) Final rubric, current-tech/current-code, and adversarial rechecks are READY with zero unresolved blocker, critical, high, or major findings; lint_spine reports zero findings.
- (event) spine finalized
- (event) Headless update resumed from the existing memlog; the spine moved to draft while corrected Capture Receipt retention and admission semantics are reconciled.
- (constraint) Corrected SPEC CAP-8 and acceptance-contract plus finalized PRD FR-5, FR-12, FR-14, NFR-4, NFR-7, and addendum sections 2, 4, 7, and 9 supersede only prior receipt-dismissal and retention wording; AD identifiers and every unrelated architecture rule remain stable.
- (decision) AD-15 now makes SessionEnd admission atomic under a per-project lock: derive identity, deduplicate the same-session receipt before accounting, then admit a genuinely new in-memory queued receipt only when prospective unresolved count and bytes both fit. Cap refusal creates no receipt or worker, performs no network, Git diff, upload, or summarizer work, says the session was not captured, emits bounded retention-pressure recovery guidance, and fails open within the foreground budget.
- (decision) AD-16 now permits expiry only for succeeded receipts after configured retention. Queued, processing, failed, retry-exhausted, and blocked-missing-baseline receipts are unresolved and never auto-deleted or silently compacted; invalid receipt state is preserved and conservatively blocks admission. Finite count and byte caps are prospective new-admission gates, not mutation or dismissal mechanisms.
- (decision) AD-17 retains finite automatic retry but makes a direct capture retry a single operator-authorized attempt on the same failed or retry-exhausted receipt; a failed direct attempt returns to retry-exhausted without an automatic loop. Blocked-missing-baseline can requeue only with a validated explicit Git reference, and retention-pressure remains a computed finding rather than a receipt state.
- (decision) Retention accounting is one shared state function under the per-project admission lock: unresolved count covers the five non-succeeded states, unresolved bytes sum their on-disk sizes, invalid entries are preserved and counted conservatively, and the canonical in-memory queued receipt byte cost is added prospectively. Existing receipt growth remains allowed within its own ceiling; aggregate pressure blocks only later admissions.
- (decision) Status and notebook.capture-receipts audit expose the same computed RetentionPressureSummaryV1 and retention-pressure finding with current count/bytes, both caps, and exact capture list and same-receipt capture retry command forms. No pressure marker is persisted and no seventh receipt state exists.
- (event) Corrected receipt-retention source reconciliation completed against SPEC CAP-8, acceptance-contract Session Capture and verification clauses, PRD FR-5/FR-12/FR-14 and non-goals/NFRs, and addendum configuration/CLI/session-flow/verification clauses; unrelated AD-1 through AD-23 rules remain unchanged.
- (event) Independent update rubric review completed NEEDS-CHANGES with two major findings: receiptless baseline lifecycle was unbounded and AD-18 used ambiguous raw session ID wording.
- (decision) RUR-1 resolved with finite receiptless_session_retention_seconds. Under the per-project state lock, a same-session close within grace re-evaluates admission from the unchanged baseline; cap refusal creates no receipt and bounded refusal metadata never extends grace. Only unreferenced receiptless baseline, claim, and refusal metadata may be pruned after grace; every baseline referenced by an unresolved receipt or journal is preserved regardless of age.
- (decision) CAP-4 side-effect-free audit remains binding while RUR-1 maintenance is added: start/end and selected owned migration may prune elapsed unreferenced receiptless state; status/audit compute and report the identical stale set and cleanup eligibility without mutation.
- (decision) RUR-2 resolved by amending AD-18 and all derivative evidence to persist only AD-15 lowercase-hex session_key provenance, never the raw client session ID.
- (event) Independent adversarial update review completed NEEDS-CHANGES with three major divergences: grace-boundary ordering, speculative status/audit candidate bytes, and incomplete state-integrity output precedence.
- (decision) A1 resolved with one locked SessionEnd order: same-receipt dedupe, then baseline eligibility where equality at created_at plus receiptless_session_retention_seconds is expired and only unreferenced receiptless state is pruned/ignored, then actual candidate serialization, integrity proof, and admission. Late close follows missing-baseline behavior.
- (decision) A2 resolved by using only the real SessionEnd candidate bytes for prospective admission. Cap refusal writes one bounded RetentionRefusalV1 event marker keyed by hashed session_key with timestamp, count-cap/byte-cap/both reason, current usage, actual candidate bytes, caps, and exact list/retry actions; repeat close replaces it, admission removes it, and status/audit report active markers without synthesizing a candidate or admission boolean.
- (decision) A3 resolved with state-integrity precedence whenever receipt count or bytes cannot be proven. Suspect entries remain untouched; the hook refuses before receipt/worker/slow work, emits no pressure marker, and hook/status/audit share nullable exact totals, lower bounds, unmeasurable count, safe entry IDs/reasons, and exact local audit plus in-place repair/re-run guidance.
- (event) Rubric recheck resolved RUR-1, RUR-2, A1, and A3 but reopened A2 as one major: an active refusal marker could keep reporting pressure after current usage recovered.
- (decision) Marker-backed status/audit now recomputes each real stored candidate byte cost against current exact usage and current caps. A failing predicate emits retention-pressure; a now-fitting marker remains visible only as capture-not-recorded until replay or grace cleanup. Recovery therefore resumes and is reported immediately below both caps without waiting for another close.
- (decision) Final RUR-A2 terminology binding: when current exact usage plus an active marker candidate now fits, the marker remains only in active_refusals with informational outcome capture-refused-history. It is never a finding or receipt state; replay may admit before grace expiry and removes the marker. State-integrity retains precedence.
- (event by architecture-update) Final update reviewer gate closed: rubric READY, current-tech/current-code READY, and adversarial divergence READY, with zero unresolved blocker, critical, high, or major findings. Pre-final lint passed with zero findings and the six-artifact diff-check passed with zero whitespace errors.
- (event by architecture-update) Spine finalized after the corrected receipt-retention update; no architecture decision IDs were renumbered or replaced.
